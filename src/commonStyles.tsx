import {css} from 'styled-components';

export const commonMdCss = css`
  p {
    margin: 0.5rem 0;
  }

  ul {
    list-style-type: none;
    margin: 0.5rem 0;
    padding-left: 1rem;
  }
  ul li {
    margin: 0.5rem 0;
  }
`;
